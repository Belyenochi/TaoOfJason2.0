node_modules/
public/
db.json
.deploy_git/
# Posts recovered from the generated site that must not be republished without
# a decision: a letter the author removed from the live site by hand in 2025, a
# work weekly report, and an empty stray draft. They live in the local working
# copy only.
_private/
# The theme itself is not vendored. NexT v5.1.4 is cloned into themes/next;
# only our _config.yml for it is tracked.
themes/next/*
!themes/next/_config.yml
